{
  "name": "amc-platfrom-b2c-platform-test",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@amc-platfrom/shared-contexts && npm run build --workspaces --if-present",
    "deploy": "npm run build && firebase deploy",
    "start": "npm run start --workspace=platform-shell",
    "start:analyzer": "npm run start --workspace=cross-analyzer-agent",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase": "^11.9.0",
    "firebase-admin": "^12.1.0"
  }
}
